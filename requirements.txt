# astk_web -- web app dependencies (only what the site actually uses).
#
# astk itself is intentionally NOT installed here: the Streamlit deployment
# only covers "transcript quantification -> PSI -> figures". The official ASTK
# package drags in gsepy==0.10.8 / pysam / nease / pyfaidx / pyecharts, which
# are heavy and not needed for the web path (core.py falls back to a built-in
# PSI implementation that matches astk.suppa.event_psi.cal_psi).
#
# Uncomment the next line only if you want the server to use ASTK's own PSI code:
# astk==0.1.4

streamlit>=1.36
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
scipy>=1.11
